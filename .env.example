# Database Configuration
DATABASE_URL=postgresql://genealogy:PASSWORD@localhost:5432/genealogy

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Google OAuth (from Google Cloud Console)
# 1. Go to https://console.cloud.google.com/
# 2. Create OAuth 2.0 credentials
# 3. Add redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# For production behind a proxy
# AUTH_TRUST_HOST=true

# Local Development - Proxy to Live API
# Set these to use the live API instead of a local database
# USE_LIVE_API=true
# GRAPHQL_PROXY_URL=https://family.milanese.life/api/graphql
# GRAPHQL_PROXY_API_KEY=your-api-key-from-admin-panel

# Local Development - Skip Auth (ONLY works when NODE_ENV=development)
# Use this to test pages without signing in via Google OAuth
# SKIP_AUTH=true

# S3 Storage Configuration (optional - configured via admin settings)
# If not set, uses IAM role for App Runner in production
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# S3_MEDIA_BUCKET=genealogy-media-123456789012
